version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
  build:
    commands:
      - echo Gerando backend/.env
      - bash scripts/gen-env.sh
      - echo Backend build
      - cd backend
      - npm ci
      - npx prisma generate
      - cd ..
      - echo Frontend build
      - cd frontend
      - npm ci
      - npm run build
      - cd ..

artifacts:
  files:
    - "**/*"
  base-directory: .
